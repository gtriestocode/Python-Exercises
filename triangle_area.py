#!/usr/bin/env python3

# Task: Calculate the are of the triangle.
# Example input: base = 45, height = 101
# Example output: 2272.5

base = int(input("What is the base? "))
height = int(input("What is the height? "))
area = base * height / 2 
print("Area of the triangle is " + str(area))
